# Review

Apply the `code-review` skill to the specified file, function, or diff.

Usage:
- `/review` — review current file open in editor
- `/review path/to/file.ts` — review specific file
- `/review --security` — security-focused review only
- `/review --performance` — performance-focused review only

Output: findings report with severity levels + score + verdict.
