# Commit and Push

Stage, commit, and push the current branch.

1. Run `git diff --staged` and `git status`
2. Apply the `git-commit` skill to generate a message
3. Create the commit
4. Run `git push origin HEAD`
5. Report the remote URL and commit hash

Ask for confirmation before pushing if branch is `main` or `master`.
